# Production environment variables
# Non-secret values may be committed here.
# Secrets (BETTER_AUTH_SECRET, MONGODB_URI, etc.) must be set in the
# deployment platform (Vercel environment variables).
